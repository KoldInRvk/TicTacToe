version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: update-npm
          command: 'npm install -g npm@latest'
      - run:
          name: install npm
          command: npm install
      - run:
          name: test
          command: npm test
